function useSuggestionFocus() {
  // const setInputValue = (value: string) => {
  //   if (!searchInputRef?.current) {
  //     return;
  //   }
  //   searchInputRef.current.value = value;
  // };
}

export default useSuggestionFocus;
